<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
      body {
        margin: 0;
        padding: 0;
        font-family: "Comic Sans MS", "Segoe UI", sans-serif;
        background: #fdf6e3;
        color: #333;
      }

      .content {
        max-width: 800px;
        margin: 50px auto;
        padding: 25px 30px;
        background: #fff;
        border: 4px solid #000;
        border-radius: 20px;
        box-shadow: 6px 6px 0 #000;
      }

      .content h2 {
        margin: 0 0 15px 0;
        font-size: 2.2rem;
        color: #0077ff;
        text-shadow: 2px 2px 0 #000;
      }

      .content p {
        margin: 8px 0;
        font-size: 1.1rem;
        color: #555;
      }

      /* Back to home button styled like content */
      .back-home {
        display: block;
        max-width: 800px;
        margin: 30px auto 50px;
        padding: 25px 30px;
        background: #bdc3c7;
        color: #000;
        text-decoration: none;
        font-weight: bold;
        font-size: 1.2rem;
        text-align: center;
        border: 4px solid #000;
        border-radius: 20px;
        box-shadow: 6px 6px 0 #000;
        transition: transform 0.1s ease, box-shadow 0.1s ease, background 0.1s ease;
      }

      .back-home:hover {
        transform: translate(2px, 2px);
        box-shadow: 2px 2px 0 #000;
        background: #95a5a6;
      }

      /* Optional: make first paragraph more “cartoony” */
      .content p:first-of-type {
        font-weight: bold;
      }
    </style>
  </head>
  <body>
    <div class="content">
      <h2><%= content[0].Title %></h2>
      <p><%= content[0].Date %></p>
      <p><%= content[0].Content %></p>
    </div>

    <a href="/" class="back-home">back to home</a>
  </body>
</html>
