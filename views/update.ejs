<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
      body {
        margin: 0;
        padding: 0;
        font-family: "Comic Sans MS", "Segoe UI", sans-serif;
        background: #fdf6e3;
        color: #333;
      }

      form {
        max-width: 700px;
        margin: 50px auto;
        padding: 25px 30px;
        background: #fff;
        border: 4px solid #000;
        border-radius: 20px;
        box-shadow: 6px 6px 0 #000;
        display: flex;
        flex-direction: column;
        gap: 20px;
      }

      input[type="text"],
      input[type="date"],
      textarea {
        padding: 12px 15px;
        font-size: 1rem;
        border: 3px solid #000;
        border-radius: 12px;
        box-shadow: 3px 3px 0 #000;
        outline: none;
        transition: transform 0.1s ease, box-shadow 0.1s ease,
          background 0.2s ease;
        width: 100%;
        box-sizing: border-box;
        font-family: "Comic Sans MS", "Segoe UI", sans-serif;
      }

      input[type="text"]:focus,
      input[type="date"]:focus,
      textarea:focus {
        transform: translate(2px, 2px);
        box-shadow: 1px 1px 0 #000;
        background: #fffbcc;
      }

      textarea {
        min-height: 150px;
        resize: vertical;
        border-radius: 16px;
      }

      input[type="submit"] {
        padding: 12px 20px;
        font-size: 1.1rem;
        font-weight: bold;
        border: 3px solid #000;
        border-radius: 16px;
        background: #2ecc71;
        color: #fff;
        cursor: pointer;
        box-shadow: 4px 4px 0 #000;
        transition: transform 0.1s ease, box-shadow 0.1s ease,
          background 0.2s ease;
      }

      input[type="submit"]:hover {
        transform: translate(2px, 2px);
        box-shadow: 2px 2px 0 #000;
        background: #27ae60;
      }

      /* Optional: make the readonly ID field subtle */
      input[readonly] {
        background: #f0f0f0;
        color: #555;
        cursor: not-allowed;
      }
    </style>
  </head>
  <body>
    <form action="/admin/update" method="get">
      <input type="text" name="ID" id="" value="<%= content[0].ID%>" readonly />
      <input
        type="text"
        placeholder="Title"
        name="title"
        value="<%= content[0].Title %>"
      />
      <input
        type="date"
        placeholder="Enter date"
        name="date"
        value="<%= content[0].Date %>"
      />
      <textarea
        name="content"
        id="content"
        placeholder="Enter your blog content"
      >
<%= content[0].Content %></textarea
      >
      <input type="submit" />
    </form>
  </body>
</html>
